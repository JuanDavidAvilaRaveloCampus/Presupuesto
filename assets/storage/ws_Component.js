self.onmessage = function nimodo(){

    let ingreso = [];
    let egreso = [];
        evento.preventDefault(); //evita que se recargue el formulario
        if (ingresar_egresar == ingreso){
            console.log(`entró al primer condicional`);

            //ingresa la data al array ingreso
            ingreso.push.value;

            console.log(`finalizó el primer condicional`);
        } else if (ingresar_egresar.value == engreso) {
            console.log(`entró al segundo condicional`);

            //ingresa la data al array de egreso
            egreso.push.value;


            console.log(`finalizó el segundo condicional`);
        }
        console.log(`teminó:D`);
};


